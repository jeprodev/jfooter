jQuery.fn.jFooterLayer = function(){   
    /* var screenObj = window.screen;
    var boxes = jQuery('.jfooter-theme-box-container');
    var boxesLength = boxes.length; 
    
    if(screenObj.width < 480){
        
    }else if(screenObj.width < 650){
        
    }else if(screenObj.width < 980){
        
    }else {
        setBoxesLayout(1000);
    }
    
    function setBoxesLayout(containerWidth){
        var boxWidth = Math.floor(containerWidth / boxesLength) - 2;
        boxes.css('width', boxWidth + 'px');
    } */
};